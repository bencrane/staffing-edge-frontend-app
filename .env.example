# Secrets managed via Doppler
# Set DOPPLER_TOKEN in Railway environment variables

# API URLs (server-side only — no NEXT_PUBLIC_ prefix)
OEX_API_BASE_URL=http://localhost:8000
DEX_API_BASE_URL=http://localhost:8001

# Cookie config
COOKIE_DOMAIN=  # Leave empty for localhost. Set to ".staffingactivation.com" in production.
NODE_ENV=development
